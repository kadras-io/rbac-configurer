#@ load("@ytt:overlay", "overlay")

#@overlay/match by=overlay.subset({"metadata":{"name":"kadras-rbac"}, "kind":"PackageInstall"})
---
spec:
  #@overlay/match missing_ok=True
  values:
    - secretRef:
        name: kadras-rbac-values
